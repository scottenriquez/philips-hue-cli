describe('Using configuration service to', () => {
    describe('obtain profile', () => {
        it('should read the correct file from the file system');
    });
});

describe('Using configuration service to', () => {
    describe('create profile', () => {
        it('should write the new profile to the file system');
    });
});