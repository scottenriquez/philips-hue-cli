describe('Using the Hue service to', () => {
    describe('initialize CLI', () => {
        it('should check to see if a connection profile exists');
        it('should create a new profile if one does not exist');
    });
});

describe('Using the Hue service to', () => {
    describe('turn on all lights', () => {
        it('should read the local bridge connection profile');
        it('should establish a connection with the bridge');
        it('should turn on all the lights');
    });
});

describe('Using the Hue service to', () => {
    describe('turn on all lights', () => {
        it('should read the local bridge connection profile');
        it('should establish a connection with the bridge');
        it('should turn off all the lights');
    });
});